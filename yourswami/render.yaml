services:
  - type: worker
    name: diamond-boys-bot
    runtime: node
    buildCommand: cd bot && npm install && npm run build
    startCommand: cd bot && npm start
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
      - key: GEMINI_API_KEY
        sync: false
