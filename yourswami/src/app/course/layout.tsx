/**
 * Course funnel layout — no navbar, no footer.
 * Pure conversion-focused experience.
 */
export default function CourseLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
